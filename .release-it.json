{
  "hooks": {
    "before:bump": "mise exec -- cargo set-version ${version}",
    "after:bump": "mise exec -- git-cliff --output CHANGELOG.md --tag ${version} && git add Cargo.toml Cargo.lock CHANGELOG.md"
  }
}

