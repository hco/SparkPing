[server]
host = "127.0.0.1"
port = 8080

[logging]
level = "debug"
file = "sparkping.log"

[database]
path = "./tsink-data"

# [ping]
# socket_type = "dgram"  # "dgram" (default, unprivileged) or "raw" (requires root)
