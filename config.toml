[server]
host = "127.0.0.1"
port = 8080

[logging]
level = "debug"
file = "sparkping.log"

[database]
path = "./tsink-data"

# [ping]
# socket_type = "dgram_native"  # "dgram_native" (default), "dgram", or "raw"
